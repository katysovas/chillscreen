/** Deep-space mid backdrop — matches Three.js scene background. */
export const SPACE_BG = '#160016';

export const CREATOR_SCENE_HREF = '/images/cities/creator-live-scene.svg?v=1';

export const SPACE_MID_TILE_W = 2600;
export const SPACE_MID_TILE_H = 900;

/** Total point count at desktop — ~6% of original demo; mobile scales down in layer. */
export const SPACE_PARTICLE_COUNT = 9000;

/** Internal WebGL resolution vs layout size (0.5 = half res, CSS upscales). */
export const SPACE_RENDER_SCALE = 0.5;

/** Cap nebula refresh — ambient backdrop does not need 60fps. */
export const SPACE_TARGET_FPS = 20;
