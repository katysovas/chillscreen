import { SeedAdmin } from '@/components/admin/SeedAdmin';

export const metadata = {
  title: 'Seed Admin',
  robots: { index: false, follow: false },
};

export default function SeedAdminPage() {
  return <SeedAdmin />;
}
