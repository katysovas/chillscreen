/** Pin easel NPCs at canvases on load and show easels before welcome dismiss (testing). */
export const TEST_EASEL_ON_LOAD = false;

/** Show paintbrush on cinema easel NPCs on load — for hand prop CSS tuning. */
export const TEST_EASEL_BRUSH_ON_LOAD = false;
