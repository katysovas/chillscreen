/** Extra downward offset for venue stages so banners fit in the viewport. */
export const STAGE_DROP_Y = 80;

/** Mid-layer y where stage decks meet the sidewalk (terrain stays at MID_GND). */
export const STAGE_ANCHOR_Y = 660 + STAGE_DROP_Y;
